{
  "name": "iforget-capacitor",
  "private": true,
  "description": "Capacitor нативна обвивка за iForget (App Store/Google Play) — виж CLAUDE.md за пълния план. Не бърка с functions/package.json (отделен npm проект, Cloud Functions).",
  "scripts": {
    "sync-www": "node scripts/sync-www.js",
    "sync": "npm run sync-www && npx cap sync",
    "open:android": "npm run sync && npx cap open android",
    "open:ios": "npm run sync && npx cap open ios",
    "icons": "capacitor-assets generate --ios --android --iconBackgroundColor '#1E2A37' --iconBackgroundColorDark '#1E2A37' --splashBackgroundColor '#1E2A37' --splashBackgroundColorDark '#1E2A37'"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.0.0"
  }
}
