# Докъде сме с пускането в магазините

Последно обновен: **02.09.2026**

Този файл съществува, защото един чат прозорец веднъж вече забѝ и отнесе
цяла вечер работа със себе си. Тук стои състоянието, за да не зависи от
паметта на разговор.

---

## Google Play — попълнено, блокирано от затворения тест

Акаунтът е потвърден, приложението е създадено (`eu.iforget.app`, име
`iForget`, Free), формулярите са попълнени. **Production е заключен** и
това не се заобикаля:

```
затворен тест → 12 тестери, записали се → 14 дни → Apply for production
```

Точният текст в конзолата: *"You must run a closed test before you can
apply to publish your app to everyone in production."* Часовникът тръгва
чак когато 12-имата са ВЪТРЕ, затова събирането на хора е истинската
критична задача, не попълването на формуляри.

### Попълнено

| Какво | Състояние |
|---|---|
| Store listing en-US + bg | ✅ от `store/store-listing-texts.md` |
| Икона, банер, 5 снимки | ✅ |
| Privacy policy | ✅ `iforget.eu/privacy.html` |
| Content rating (IARC) | ✅ All Other App Types |
| Target audience | ✅ 18+, не е за деца |
| Ads | ✅ няма реклами |
| Advertising ID | ✅ **не се ползва** — проверено в `android/app/build.gradle`: няма нито рекламна, нито аналитична библиотека |
| Health apps | ✅ без Health Connect |
| Data safety | ✅ виж по-долу |
| Closed testing "Alpha" | държави ✅, тестери ⏳, версия ⏳ |

### Data safety — какво е декларирано

Четири типа, всичките само **Collected**, никога **Shared** (Firebase е
доставчик на услугата от твое име, което изрично е изключение):

| Тип | Къде | Задължително |
|---|---|---|
| Email address | вход/акаунт | по избор (виж бележката за режима без акаунт) |
| User IDs | Firebase `uid` | по избор |
| Other info | текстът на бележките | по избор |
| **Health info** | `cycleData`, месечен цикъл | по избор |

Не се отмята нищо от Messages (Emails / Other in-app messages) — това са
съдържание на чужди писма и чат между потребители, каквито iForget няма.
Веднъж вече бяха отметнати по погрешка и трябваше да се махат.

⚠️ **Режимът без акаунт променя два отговора** (промяната дойде от App
Store Guideline 5.1.1(v), но важи и тук — същият `index.html`):

- **App access** — приложението вече е напълно използваемо без вход,
  значи истинският отговор е "всичко е достъпно без специален достъп".
  Тестовият акаунт може да остане като бележка към ревюиращия, за да
  провери и синхронизацията, но не е задължителен.
- **Data safety → required/optional** — щом акаунтът е по избор, имейлът,
  `uid`-то и бележките се събират само ако човек сам реши да се
  регистрира. Значи **"Users can choose whether this data is collected"**,
  не "required".

### Проверено на реално устройство (Samsung Galaxy A17, Android 16)

- Приложението тръгва нативно.
- **Напомнянията звънят при НАПЪЛНО затворен ап.**
- Баджът върху иконата се изчиства при отваряне
  (`clearDeliveredNativeNotifications()`).

⚠️ Проверката е от **22.08**, ПРЕДИ режима без акаунт. Новият билд минава
пак през телефон — поуката от iOS (два бъга, невидими в Chromium) важи и
за Android.

### Пакетът — пресглобява се преди ВСЯКО качване

Файлът на диска остарява при всяка промяна в `index.html`, а това се
случва почти всеки ден. Пресглобяването е три минути; качен стар пакет е
две седмици затворен тест наново.

```
cd ~/iforget && git pull origin main && npm run sync
cd android && ./gradlew bundleRelease
```

Проверявай, че НАЙ-НОВАТА промяна наистина е ВЪТРЕ в пакета, не само в
repo-то — има ли нея, има и всички преди нея:

```
unzip -p app/build/outputs/bundle/release/app-release.aab \
  base/assets/public/index.html | grep -c "'monthly'"
```

Реален капан от миналото: `npm run sync` копира ЛОКАЛНИЯ файл, а
`git pull` беше пропуснат — build-ът минава, приложението работи, само
поправката липсва. Случи се два пъти и в тази сесия: пакет от 24.08 без
режима без акаунт, после пакет от 31.08 без месечните напомняния.

`versionName` е **1.0.1** (изравнено с iOS), `versionCode` е **1**. Щом
версия с даден `versionCode` веднъж е качена в кой да е трак, следващата
иска по-голям номер в `android/app/build.gradle` — Play отказва повторно
качване със същия.

### Какво остава

1. **Двете поправки в Play Console заради режима без акаунт** (виж
   Data safety по-горе): App access → „без специален достъп"; Email
   address / User IDs / Other info → „по избор" вместо „задължително".
2. Closed testing → Alpha → **Create a new release** → качваш `.aab` →
   release notes → Save → Review release → **Start rollout**. Само „Save"
   не пуска нищо.
   ⚠️ Release notes се пишат ВЪТРЕ в самата версия, с падащо меню за
   език — НЕ в „Store listing" (там е описанието на приложението).
   Готовите текстове за двата езика: `store/release-notes-1.0.1.md`.
3. **Select testers** (email list) → разпращаш opt-in линка. Тестерите
   трябва да са **Google акаунти** и да останат записани през целите 14
   дни — покани 15 души за резерв.
4. След 14-те дни: **Apply for production** (има и въпросник за самия
   тест) → чак тогава публично пускане.

Редът между 2 и 3 е без значение технически, но версията се качва първа:
прегледът ѝ отнема дни и върви успоредно със събирането на хора.

Снимките в `store/screenshots/` вече са **седем** (добавени „Продължи без
акаунт" и прегледът). В Play Console са качени старите пет — сменят се по
всяко време, дори след пускането, не блокират нищо.

⚠️ **Новият билд не е пускан на Android устройство.** Последната проверка
на телефон е от 22.08, преди режима без акаунт и двете форми с парола.
Поуката от iOS (два бъга, невидими в Chromium) важи и тук — затвореният
тест е точно моментът да се хване подобно нещо.

---

## App Store — ✅ ОДОБРЕНО И ПУСНАТО (28.08.2026)

`iForget - Simple Notes`, версия 1.0, build **1.0 (7)**. Пуснато ръчно за
разпространение от собственика.

**Локален build остава невъзможен** — MacBook Air 2015 е спрян на macOS
Monterey → максимум Xcode 14.2 → Capacitor 8 иска Xcode 15+. Всяка бъдеща
версия минава през `.github/workflows/ios-release.yml` в GitHub Actions.
Пуска се ръчно от Actions → "iOS release (TestFlight)" → Run workflow.

### Двата кръга ревю — какво поискаха и защо

**1. Guideline 2.1 — Information Needed.** НЕ отказ заради бъг: Apple иска
видеозапис от реално устройство плюс писмени отговори на 7 въпроса, преди
изобщо да започне ревюто. Рутинно за нов акаунт. Готовият текст и целият
контекст са в `store/app-review-notes.md` (⚠️ полето приема най-много 4000
знака; видеото се дава като линк — YouTube **Unlisted**, не Private).

**2. Guideline 5.1.1(v) — не може да се иска регистрация за функции, които
не зависят от акаунт.** Това беше истинска, основателна промяна: при
iForget писането на бележки винаги е било локално (`save()` пише в
localStorage синхронно, Firestore записът е под `if(currentUser)`) —
липсваше само врата към това състояние. Оттам режимът без акаунт: бутон
"Продължи без акаунт", `guestMode` в localStorage и `mergeGuestNotes()`,
което слива локалните с облачните при по-късен вход, за да не се губят.

⚠️ **Помни го при всяка бъдеща функция:** ако нещо ново работи и без
акаунт, не бива да се заключва зад вход. Същото правило важи и занапред.

### Историята на билдовете (за да не се повтарят грешките)

| Build | Какво носеше / защо отпадна |
|---|---|
| 3 | iPhone+iPad, Missing Compliance — заменен |
| 4 | само iPhone + декларация за криптиране; с него е потвърдено, че приложението РАБОТИ на iPhone |
| 5 | линковете privacy/terms в нативния апп + полето "Потвърди парола" зад клавиатурата |
| 6 | режим без акаунт (междинен, не е ползван) |
| **7** | **+ оформен бутон за вход в профила — ОДОБРЕНИЯТ** |

### Потвърдено на реален iPhone (13 Pro Max, iOS 26.6)

- **напомнянията звънят при НАПЪЛНО затворено приложение** — както на Android;
- клавиатурата в трите сценария (нова бележка, подбележка, редакция);
- жестовете: плъзгане наляво/надясно, задържане;
- вход, регистрация, работа без акаунт.

**Поуката, повторена два пъти:** Chromium не възпроизведе НИТО ЕДИН от
бъговете, намерени на устройството. Нативната обвивка е друга среда и
единственият надежден тест е реален телефон.

### Настройки на акаунта (референция)

| Какво | Стойност |
|---|---|
| Apple Developer | Individual, Team ID `YU6TSUJ9A4` |
| Bundle ID | `eu.iforget.app` |
| Име в магазина | `iForget - Simple Notes` (чистото `iForget` е заето) |
| Език на описанието | английски (App Store Connect не предлага български) |
| Устройства | само iPhone (`TARGETED_DEVICE_FAMILY = 1`) |
| Цена | Free, 175 държави |
| Age Rating | 4+ |
| App Privacy | имейл, uid, бележки, здравна информация |
| Trader status (DSA) | Active, 27 държави от ЕС |
| Пускане | ръчно |

### При следваща версия

1. `git pull` → промените в `index.html`
2. Actions → "iOS release (TestFlight)" → Run workflow (номерът расте сам)
3. TestFlight тест на устройство — ⚠️ **не пропускай**, виж поуката горе
4. App Store Connect → нова версия → избери билда → Submit

⚠️ **Android пакетът на диска е ОСТАРЯЛ.** Всички промени от 24–28.08.2026
важат и за Android (същият `index.html`). Преди качване в Google Play:
`git pull` → `npm run sync` → `./gradlew bundleRelease`.

---

## Отложено, не блокира нищо

- App Check / reCAPTCHA срещу ботове при регистрация
- Преглед на ru/de/es от носител (писани са от Клод)
- Юридически преглед на `privacy.html`/`terms.html`
- Premium/Stripe — готов, скрит зад `config/premium.enabled` във Firestore
- Споделени бележки между акаунти
