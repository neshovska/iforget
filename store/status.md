# Докъде сме с пускането в магазините

Последно обновен: **23.08.2026**

Този файл съществува, защото един чат прозорец веднъж вече забѝ и отнесе
цяла вечер работа със себе си. Тук стои състоянието, за да не зависи от
паметта на разговор.

---

## Google Play — ГОТОВО, чака Google

**Блокирано е на едно нещо:** Google проверява самоличността на
собственика на акаунта (`neshovska`, Personal account). Документите са
качени, чака се имейл. Отнема до няколко дни.

Редът е строг и нищо не може да се прескочи:

```
самоличност (Google) → телефон (2 мин, ти) → Create app
```

Бутонът "Create app" стои заключен с надпис *"Complete account
verifications to create new apps"*, а потвърждаването на телефона също е
сиво, докато самоличността не мине.

### Готово от наша страна

| Какво | Къде |
|---|---|
| Ключ за подписване | `~/iforget-upload.jks` (НЕ в repo-то) |
| Настройка към ключа | `android/keystore.properties` (в `.gitignore`) |
| Подписан `.aab` | `android/app/build/outputs/bundle/release/app-release.aab` |
| Икона 512×512 | `icon-512.png` (в корена) |
| Банер 1024×500 | `store/banner.png` |
| 5 снимки 1290×2796 | `store/screenshots/` |
| Описания бг + en | `store/store-listing-texts.md` |
| Отговори за Data safety | същия файл, секция "Data safety" |
| Стъпки за качване | `store/google-play.md` |

### Проверено на реално устройство (Samsung Galaxy A17, Android 16)

- Приложението тръгва нативно.
- **Напомнянията звънят при НАПЪЛНО затворен ап** — последното
  непроверено нещо в проекта.
- Баджът върху иконата се изчиства при отваряне на приложението
  (виж `clearDeliveredNativeNotifications()`).

### Когато дойде имейлът

1. Потвърди телефона (Developer account → Account details)
2. Create app → `iForget`, **английски за основен език**, App, Free —
   така приложението е разбираемо навсякъде по света; българският се
   добавя веднага след това като допълнителен превод (Google Play
   поддържа български, за разлика от App Store).
3. Store listing — копирай от `store/store-listing-texts.md`, и двата
   езика
4. Data safety — ⚠️ **не пропускай здравната информация** (месечния
   цикъл, `cycleData`). Пропуснато деклариране е причина за сваляне на
   приложението после, не при първото ревю.
5. Content rating — кратък въпросник
6. Качи `.aab` → Rollout

⚠️ **Преди качване: `git pull` и пресглоби.** Файлът на диска остарява при
всяка промяна в кода:

```
cd ~/iforget && git pull origin main && npm run sync
cd android && ./gradlew bundleRelease
```

Провери, че поправките наистина са ВЪТРЕ в пакета, не само в repo-то:

```
unzip -p app/build/outputs/bundle/release/app-release.aab \
  base/assets/public/index.html | grep -c clearDeliveredNativeNotifications
```

Това е реален капан — веднъж вече беше инсталиран APK без поправка,
защото `npm run sync` копира ЛОКАЛНИЯ файл, а `git pull` беше пропуснат.
Симптомът е коварен: build-ът минава, приложението работи, само
поправката липсва.

⚠️ **Провери в Play Console дали важи изискване за затворен тест** с
тестери преди публично пускане (раздел Testing). За нови лични акаунти
такова правило беше въведено; числата се менят. Ако важи, тръгва ПЪРВО —
върви по календар и добавя седмици.

---

## App Store — build-ът е ГОРЕ, чака TestFlight тест

**Локален build е невъзможен.** MacBook Air 2015 е спрян на macOS
Monterey → максимум Xcode 14.2 → Capacitor 8 иска Xcode 15+. Няма
безплатно решение на тази машина. Всичко минава през GitHub Actions.

### Готово

| Какво | Състояние |
|---|---|
| Apple Developer, Individual | ✅ одобрен, Team ID `YU6TSUJ9A4` |
| Bundle ID `eu.iforget.app` | ✅ |
| Сертификат + provisioning profile | ✅ (`~/iforget-ios-keys`, НЕ в repo-то) |
| 7 GitHub Secrets | ✅ зададени |
| Компилация за симулатор | ✅ `ios-build.yml` |
| Подписан build + качване | ✅ `ios-release.yml`, потвърдено 23.08.2026 |
| Приложение в App Store Connect | ✅ `iForget - Simple Notes` |
| Снимки + описание (английски) | ✅ качени |
| Export compliance (криптиране) | ✅ решено в `Info.plist` |

**Защо името е различно:** `iForget` е заето в App Store, затова там е
`iForget - Simple Notes`. В Google Play си остава `iForget`. App Store
Connect не предлага български език за описанието — листингът там е на
английски.

### Какво остава

1. **Изчакай Apple да обработи build-а** (5–30 мин след качването), после
   го избери в раздела Build на версията.
2. **TestFlight тест на реален iPhone** — това е единственият останал
   истински въпрос по проекта. Провери:
   - звънят ли напомнянията при затворен ап (на iOS таванът е 64 чакащи
     известия, затова кодът насрочва първите 50);
   - клавиатурата в трите сценария (нова бележка, подбележка, редакция) —
     за уеб е решено след дълга история, но нативната обвивка е друга среда;
   - жестовете: плъзгане наляво/надясно, задържане;
   - влизане с имейл и парола.
3. **App Privacy декларация** — ⚠️ включително **здравна информация**
   (месечният цикъл, `cycleData`).
4. **Trader status** (Digital Services Act) → App Store Connect →
   Business. Изисква се за разпространение в ЕС.
5. Add for Review → Submit.

**НЕПРОВЕРЕНО:** дали приложението работи на iOS. Компилация и успешно
качване ≠ работещо приложение.

**Междувременно iPhone потребителите не са без нищо** — iforget.eu се
добавя на Home Screen като PWA и изглежда почти като приложение. Липсват
само напомнянията при затворен ап.

## Отложено, не блокира нищо

- App Check / reCAPTCHA срещу ботове при регистрация
- Преглед на ru/de/es от носител (писани са от Клод)
- Юридически преглед на `privacy.html`/`terms.html`
- Premium/Stripe — готов, скрит зад `config/premium.enabled` във Firestore
- Споделени бележки между акаунти
